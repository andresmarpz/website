import { styled } from "@/stitches.config";

const Paragraph = styled('p', {
    margin: 0,
    color: '$slate12',

    lineHeight: 1.4,
    fontSize: '16px',
    fontWeight: 'normal'
});

export default Paragraph